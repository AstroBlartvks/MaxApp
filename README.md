# Max Photo Gallery - Docker Setup

🎨 **Проект фотогалереи для мессенджера Max** с управлением цифровым искусством, включая оригиналы и дубликаты.

## 📋 Содержание

- [Архитектура проекта](#архитектура-проекта)
- [Быстрый старт](#быстрый-старт)
- [Требования](#требования)
- [Установка и настройка](#установка-и-настройка)
- [Запуск проекта](#запуск-проекта)
- [Полезные команды](#полезные-команды)
- [Структура проекта](#структура-проекта)
- [Переменные окружения](#переменные-окружения)
- [Troubleshooting](#troubleshooting)

---

## 🏗️ Архитектура проекта

Проект состоит из трех основных компонентов:

```
┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│  Frontend   │─────▶│   Backend   │─────▶│  PostgreSQL │
│   (React)   │◀─────│  (FastAPI)  │◀─────│  Database   │
│   Port 80   │      │  Port 8000  │      │  Port 5432  │
└─────────────┘      └─────────────┘      └─────────────┘
```

### Технологический стек:

- **Frontend**: React 18 + Vite + Bootstrap 5 + Max UI
- **Backend**: FastAPI (Python 3.11) + asyncpg + JWT
- **Database**: PostgreSQL 16
- **Infrastructure**: Docker + Docker Compose + Nginx

---

## 🚀 Быстрый старт

### Вариант 1: Docker Compose (рекомендуется)

```bash
# 1. Клонируйте репозиторий
git clone <repository-url>
cd Proj

# 2. Создайте файл окружения
cp .env.docker.example .env

# 3. Отредактируйте .env (минимум измените пароли и токены!)
nano .env

# 4. Запустите все сервисы
docker-compose up -d

# 5. Проверьте статус
docker-compose ps

# 6. Откройте в браузере
# Frontend: http://localhost
# Backend API Docs: http://localhost:8000/docs
```

### Вариант 2: Локальная разработка

См. раздел [Запуск без Docker](#запуск-без-docker)

---

## 📦 Требования

### Для Docker:
- **Docker**: версия 20.10+
- **Docker Compose**: версия 2.0+
- **Свободные порты**: 80, 8000, 5432
- **Оперативная память**: минимум 2GB
- **Свободное место**: минимум 5GB

### Для локальной разработки:
- **Python**: 3.11+
- **Node.js**: 20+
- **PostgreSQL**: 16+
- **npm**: 9+

---

## 🔧 Установка и настройка

### Шаг 1: Подготовка переменных окружения

```bash
# Скопируйте пример конфигурации
cp .env.docker.example .env
```

### Шаг 2: Редактирование .env файла

Откройте `.env` и **обязательно измените**:

```env
# ⚠️ КРИТИЧЕСКИ ВАЖНО: Измените пароль БД!
DB_PASSWORD=your_strong_password_here

# ⚠️ ОБЯЗАТЕЛЬНО: Добавьте токен бота
BOT_TOKEN=your_bot_token_from_max_messenger

# 🔐 Рекомендуется: Сгенерируйте новые секретные ключи
# Используйте: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=<сгенерированный_ключ>
REFRESH_SECRET_KEY=<другой_сгенерированный_ключ>
```

### Шаг 3: Настройка для Production (опционально)

Если разворачиваете на сервере с доменом:

```env
# Домены
BASE_URL=https://api.yourdomain.com
CORS_ORIGINS=https://yourdomain.com,https://api.yourdomain.com

# Frontend
VITE_API_URL=https://api.yourdomain.com
VITE_WS_URL=wss://api.yourdomain.com/ws
```

---

## 🎯 Запуск проекта

### Запуск с Docker Compose

```bash
# Сборка и запуск всех контейнеров
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f

# Просмотр логов конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f db
```

### Проверка работоспособности

После запуска проверьте доступность сервисов:

```bash
# Проверка базы данных
docker-compose exec db psql -U app_user -d app_db -c "\dt"

# Проверка backend API
curl http://localhost:8000/health
# Должно вернуть: {"status":"healthy"}

# Проверка frontend
curl http://localhost
# Должна вернуться HTML страница

# Или откройте в браузере:
# Frontend: http://localhost
# API Documentation: http://localhost:8000/docs
```

### Остановка проекта

```bash
# Остановить контейнеры (данные сохранятся)
docker-compose stop

# Остановить и удалить контейнеры (данные сохранятся в volumes)
docker-compose down

# Остановить и удалить контейнеры вместе с volumes (⚠️ удалит базу данных!)
docker-compose down -v
```

---

## 🛠️ Полезные команды

### Docker Compose

```bash
# Просмотр статуса контейнеров
docker-compose ps

# Перезапуск всех сервисов
docker-compose restart

# Перезапуск конкретного сервиса
docker-compose restart backend

# Пересборка конкретного сервиса
docker-compose up -d --build backend

# Просмотр логов с timestamps
docker-compose logs -f --timestamps

# Выполнение команды в контейнере
docker-compose exec backend bash
docker-compose exec db psql -U app_user -d app_db
docker-compose exec frontend sh
```

### Управление базой данных

```bash
# Подключение к PostgreSQL
docker-compose exec db psql -U app_user -d app_db

# Резервное копирование БД
docker-compose exec db pg_dump -U app_user app_db > backup_$(date +%Y%m%d_%H%M%S).sql

# Восстановление из бэкапа
cat backup.sql | docker-compose exec -T db psql -U app_user -d app_db

# Просмотр таблиц
docker-compose exec db psql -U app_user -d app_db -c "\dt"

# Просмотр пользователей
docker-compose exec db psql -U app_user -d app_db -c "SELECT * FROM users LIMIT 10;"
```

### Очистка и обслуживание

```bash
# Очистка неиспользуемых Docker ресурсов
docker system prune -a

# Просмотр размера volumes
docker volume ls
docker volume inspect max_gallery_postgres_data
docker volume inspect max_gallery_uploads

# Просмотр использования ресурсов
docker stats
```

---

## 📁 Структура проекта

```
Proj/
├── backend/                  # Backend API (FastAPI)
│   ├── app/
│   │   ├── main.py          # Точка входа приложения
│   │   ├── routers/         # API endpoints
│   │   ├── security.py      # JWT и аутентификация
│   │   └── schemas.py       # Pydantic модели
│   ├── postgresql/          # Скрипты БД и миграции
│   │   ├── database.py      # Подключение к БД
│   │   └── tables.sql       # SQL схемы
│   ├── uploads/             # Загруженные файлы
│   ├── .env                 # Переменные окружения backend
│   ├── requirements.txt     # Python зависимости
│   ├── Dockerfile           # Docker образ backend
│   └── README.md            # Документация backend
│
├── front/                   # Frontend (React + Vite)
│   ├── src/                 # Исходный код React
│   ├── public/              # Статические ресурсы
│   ├── .env                 # Переменные окружения frontend
│   ├── package.json         # Node.js зависимости
│   ├── vite.config.js       # Конфигурация Vite
│   ├── Dockerfile           # Docker образ frontend
│   ├── nginx.conf           # Конфигурация Nginx
│   └── README.md            # Документация frontend
│
├── docker-compose.yml       # Оркестрация Docker контейнеров
├── .env.docker.example      # Пример переменных для Docker
├── .env                     # Переменные окружения (создайте!)
├── ENV_SETUP.md             # Руководство по настройке .env
└── README.md                # Этот файл
```

---

## 🔐 Переменные окружения

### Критически важные переменные

| Переменная | Описание | Пример |
|------------|----------|--------|
| `DB_PASSWORD` | Пароль БД | `strong_password_123` |
| `SECRET_KEY` | Секретный ключ для JWT | `96eac3aa78e834b...` |
| `REFRESH_SECRET_KEY` | Ключ для refresh токенов | `fdf58e4ea49f5d...` |
| `BOT_TOKEN` | Токен бота Max | `ваш_токен_здесь` |

### Дополнительные переменные

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `DB_NAME` | Имя базы данных | `app_db` |
| `DB_USER` | Пользователь БД | `app_user` |
| `BASE_URL` | URL backend API | `http://localhost:8000` |
| `CORS_ORIGINS` | Разрешенные домены | `http://localhost` |
| `VITE_API_URL` | URL API для frontend | `http://localhost:8000` |
| `DEBUG` | Режим отладки | `false` |

Подробное описание всех переменных см. в [ENV_SETUP.md](ENV_SETUP.md)

---

## 📝 Запуск без Docker

### Backend (FastAPI)

```bash
cd backend

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt

# Настройте .env
cp .env.example .env
nano .env

# Установите PostgreSQL и создайте БД
# См. backend/README.md для деталей

# Запустите сервер
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

### Frontend (React)

```bash
cd front

# Установите зависимости
npm install

# Настройте .env
cp .env.example .env
nano .env

# Запустите dev-сервер
npm run dev

# Или соберите для production
npm run build
npm run preview
```

---

## 🐛 Troubleshooting

### Проблема: Порт уже занят

**Ошибка**: `Error starting userland proxy: listen tcp4 0.0.0.0:80: bind: address already in use`

**Решение**:
```bash
# Проверьте какой процесс использует порт
lsof -i :80  # Linux/Mac
netstat -ano | findstr :80  # Windows

# Измените порт в docker-compose.yml
# frontend:
#   ports:
#     - "8080:80"  # Используйте 8080 вместо 80
```

### Проблема: База данных не инициализируется

**Симптомы**: Backend не может подключиться к БД

**Решение**:
```bash
# Проверьте логи БД
docker-compose logs db

# Пересоздайте volume БД
docker-compose down -v
docker-compose up -d

# Проверьте, что SQL файл существует
ls -la backend/database.sql
```

### Проблема: Frontend не может подключиться к Backend

**Симптомы**: Ошибки CORS или сетевые ошибки в браузере

**Решение**:
```bash
# Проверьте CORS_ORIGINS в .env
# Должен включать origin frontend'а

# Для локальной разработки:
CORS_ORIGINS=http://localhost,http://localhost:80,http://localhost:8000

# Проверьте что backend доступен
curl http://localhost:8000/health

# Проверьте VITE_API_URL во frontend/.env
VITE_API_URL=http://localhost:8000
```

### Проблема: Изменения в коде не применяются

**Решение**:
```bash
# Пересоберите образы
docker-compose up -d --build

# Или пересоберите конкретный сервис
docker-compose up -d --build backend

# Очистите кэш Docker
docker builder prune
```

### Проблема: Недостаточно памяти

**Симптомы**: Контейнеры падают, OOM errors

**Решение**:
```bash
# Проверьте использование ресурсов
docker stats

# Увеличьте память для Docker Desktop
# Settings -> Resources -> Memory -> 4GB+

# Ограничьте память для контейнеров в docker-compose.yml
# services:
#   backend:
#     mem_limit: 512m
```

### Проблема: Логи показывают "Connection refused"

**Решение**:
```bash
# Проверьте что все сервисы запущены
docker-compose ps

# Проверьте healthcheck
docker inspect max_gallery_backend | grep Health

# Дождитесь полной инициализации
# Backend запускается после БД (depends_on + healthcheck)
docker-compose logs -f backend
```

---

## 🔒 Безопасность

### Для production развертывания:

1. **Измените все пароли и секретные ключи**
   ```bash
   # Генерация сильных ключей
   python -c "import secrets; print(secrets.token_hex(32))"
   openssl rand -hex 32
   ```

2. **Используйте HTTPS**
   - Настройте SSL сертификаты (Let's Encrypt)
   - Используйте reverse proxy (nginx, Traefik)

3. **Настройте firewall**
   ```bash
   # Разрешите только необходимые порты
   ufw allow 80/tcp
   ufw allow 443/tcp
   ufw deny 5432/tcp  # БД не должна быть доступна извне
   ```

4. **Регулярные обновления**
   ```bash
   # Обновление образов
   docker-compose pull
   docker-compose up -d
   ```

5. **Резервное копирование**
   ```bash
   # Настройте автоматический backup БД
   # Используйте cron для регулярных бэкапов
   ```

---

## 📚 Дополнительная документация

- [Backend Documentation](backend/README.md) - Детали API и разработки backend
- [Frontend Documentation](front/README.md) - Детали frontend разработки
- [Environment Setup](ENV_SETUP.md) - Подробное руководство по настройке .env
- [Database Setup](backend/DATABASE_SETUP.md) - Руководство по настройке БД

---