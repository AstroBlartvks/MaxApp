# 🪟 Windows Quick Guide - Max Photo Gallery

## Специальное руководство для Windows пользователей

### 📋 Предварительные требования

1. **Установите Docker Desktop для Windows**
   - Скачайте: https://www.docker.com/products/docker-desktop/
   - Запустите установщик
   - Перезагрузите компьютер после установки
   - Запустите Docker Desktop

2. **Проверьте установку**
   ```cmd
   docker --version
   docker-compose --version
   ```

---

## 🚀 Быстрый запуск (3 простых шага)

### Шаг 1: Откройте командную строку

Нажмите `Win + R`, введите `cmd` и нажмите Enter

### Шаг 2: Перейдите в папку проекта

```cmd
cd C:\Users\AstroBlartvks\1111\Proj
```

### Шаг 3: Запустите проект

```cmd
start.bat
```

Готово! Откройте браузер и зайдите на http://localhost

---

## 📝 Настройка (если это первый запуск)

### 1. Создайте файл .env

Двойной клик на файл `.env.docker.example` и скопируйте его как `.env`:

```cmd
copy .env.docker.example .env
```

### 2. Откройте .env в блокноте

```cmd
notepad .env
```

### 3. Измените эти строки:

```env
# Придумайте сильный пароль (минимум 16 символов)
DB_PASSWORD=MyStrongPassword123!@#

# Вставьте токен от бота Max
BOT_TOKEN=ваш_токен_здесь
```

### 4. Сохраните и закройте блокнот

Файл → Сохранить → Закрыть

---

## 🎮 Управление проектом

### Запуск проекта
```cmd
start.bat
```
После запуска откройте: http://localhost

### Остановка проекта
```cmd
stop.bat
```

### Просмотр логов
```cmd
logs.bat
```

Для просмотра логов конкретного сервиса:
```cmd
logs.bat backend
logs.bat frontend
logs.bat db
```

### Резервная копия базы данных
```cmd
backup.bat
```

Backup сохранится в папке `backups\`

---

## 🔧 Docker Desktop - Полезные функции

### Просмотр контейнеров

1. Откройте Docker Desktop
2. Перейдите в раздел "Containers"
3. Увидите все запущенные контейнеры:
   - max_gallery_frontend
   - max_gallery_backend
   - max_gallery_db

### Просмотр логов через GUI

1. В Docker Desktop нажмите на контейнер
2. Перейдите на вкладку "Logs"
3. Читайте логи в реальном времени

### Остановка через GUI

1. Выберите контейнер
2. Нажмите кнопку "Stop" (квадратик)

---

## 🌐 Доступ к сервисам

После запуска `start.bat` откройте в браузере:

- **Frontend приложение**: http://localhost
- **API документация**: http://localhost:8000/docs
- **Health check**: http://localhost:8000/health

---

## 🛠️ Полезные команды для Windows

### Проверка статуса
```cmd
docker-compose ps
```

### Перезапуск всех сервисов
```cmd
docker-compose restart
```

### Перезапуск одного сервиса
```cmd
docker-compose restart backend
```

### Просмотр использования ресурсов
```cmd
docker stats
```

### Подключение к базе данных
```cmd
docker-compose exec db psql -U app_user -d app_db
```

Команды SQL:
```sql
\dt                    -- Список таблиц
\d users              -- Структура таблицы users
SELECT * FROM users;  -- Показать всех пользователей
\q                    -- Выход
```

---

## 📊 Мониторинг

### Проверка работоспособности

Откройте PowerShell и выполните:

```powershell
# Проверка Backend
(Invoke-WebRequest -Uri http://localhost:8000/health).Content

# Проверка Frontend
Invoke-WebRequest -Uri http://localhost
```

Или просто откройте http://localhost:8000/health в браузере

---

## 🐛 Решение проблем на Windows

### Проблема: Docker Desktop не запускается

**Решение:**
1. Убедитесь, что включена виртуализация в BIOS
2. Включите WSL 2 (Windows Subsystem for Linux)
3. Перезагрузите компьютер
4. Запустите Docker Desktop от имени администратора

### Проблема: Порт 80 занят

**Ошибка:** "Port 80 is already in use"

**Решение:**
1. Откройте `docker-compose.yml` в блокноте
2. Найдите строку `"80:80"`
3. Измените на `"8080:80"`
4. Сохраните файл
5. Запустите заново: `start.bat`
6. Теперь открывайте: http://localhost:8080

### Проблема: База данных не инициализируется

**Решение:**
```cmd
docker-compose down -v
docker-compose up -d
```

### Проблема: Медленная работа Docker

**Решение:**
1. Откройте Docker Desktop
2. Settings → Resources
3. Увеличьте Memory до 4GB
4. Увеличьте CPUs до 2-4
5. Нажмите "Apply & Restart"

### Проблема: "Access denied" или "Permission denied"

**Решение:**
1. Запустите командную строку от имени администратора
2. Правой кнопкой на "Command Prompt" → "Run as administrator"
3. Выполните команды заново

### Проблема: Файлы не обновляются

**Решение:**
```cmd
docker-compose down
docker-compose up -d --build
```

---

## 📂 Где что находится

### Проект
```
C:\Users\AstroBlartvks\1111\Proj\
```

### Логи Backend
```
C:\Users\AstroBlartvks\1111\Proj\backend\error.log
```

### Резервные копии
```
C:\Users\AstroBlartvks\1111\Proj\backups\
```

### Загруженные файлы
Хранятся в Docker volume (управляется Docker Desktop)

---

## 💾 Резервное копирование

### Автоматический backup

Запустите:
```cmd
backup.bat
```

Backup сохранится в папку `backups\` с именем вида:
```
backup_20251115_143022.sql
```

### Восстановление из backup

```cmd
type backups\backup_20251115_143022.sql | docker-compose exec -T db psql -U app_user -d app_db
```

---

## 🔄 Обновление проекта

Если получили новую версию:

```cmd
REM 1. Остановите текущую версию
stop.bat

REM 2. Создайте backup
backup.bat

REM 3. Обновите код (если из Git)
git pull

REM 4. Пересоберите образы
docker-compose build

REM 5. Запустите новую версию
start.bat
```

---

## 🎓 Дополнительно

### Запуск в режиме разработки

Для разработки с автоматической перезагрузкой:

```cmd
docker-compose -f docker-compose.dev.yml up
```

### Очистка Docker

Если накопилось много неиспользуемых образов:

```cmd
docker system prune -a
```

⚠️ Внимание: это удалит все неиспользуемые образы!

